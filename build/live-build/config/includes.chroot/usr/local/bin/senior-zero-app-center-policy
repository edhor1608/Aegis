#!/usr/bin/env bash
set -euo pipefail

TARGET="/usr/local/sbin/senior-zero-app-center-policy"
if [[ ! -x "$TARGET" ]]; then
  TARGET="$(cd "$(dirname "${BASH_SOURCE[0]}")/../sbin" && pwd)/senior-zero-app-center-policy"
fi

exec "$TARGET" "$@"
