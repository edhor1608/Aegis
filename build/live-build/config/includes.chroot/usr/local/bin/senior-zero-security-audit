#!/usr/bin/env bash
set -euo pipefail

TARGET="/usr/local/sbin/senior-zero-security-audit"
if [[ ! -x "$TARGET" ]]; then
  TARGET="$(cd "$(dirname "${BASH_SOURCE[0]}")/../sbin" && pwd)/senior-zero-security-audit"
fi

exec "$TARGET" "$@"
