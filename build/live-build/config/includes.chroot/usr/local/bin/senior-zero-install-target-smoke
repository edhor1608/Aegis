#!/usr/bin/env bash
set -euo pipefail

TARGET="/usr/local/sbin/senior-zero-install-target-smoke"
if [[ ! -x "$TARGET" ]]; then
  TARGET="$(cd "$(dirname "${BASH_SOURCE[0]}")/../sbin" && pwd)/senior-zero-install-target-smoke"
fi

exec "$TARGET" "$@"
