#!/usr/bin/env bash
set -euo pipefail

TARGET="/usr/local/sbin/senior-zero-rollback-bootstrap"
if [[ ! -x "$TARGET" ]]; then
  TARGET="$(cd "$(dirname "${BASH_SOURCE[0]}")/../sbin" && pwd)/senior-zero-rollback-bootstrap"
fi

exec "$TARGET" "$@"
