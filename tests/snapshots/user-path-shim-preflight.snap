#!/usr/bin/env bash
set -euo pipefail

TARGET="/usr/local/sbin/senior-zero-preflight-report"
if [[ ! -x "$TARGET" ]]; then
  TARGET="$(cd "$(dirname "${BASH_SOURCE[0]}")/../sbin" && pwd)/senior-zero-preflight-report"
fi

exec "$TARGET" "$@"
